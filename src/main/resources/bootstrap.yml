spring:
  cloud:
    kubernetes:
      config:
        enabled: true
        sources:
          - namespace: default
            name: market-place-products
      reload:
        enabled: true
        mode: event
        strategy: refresh